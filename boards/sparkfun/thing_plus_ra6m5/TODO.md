To do
=====

- Set up a GPIO hog for VREF_EN
- ADC to use VREF as 3.3V reference
- SDHC, it appears there is no device configured in the SoC dts. Add the SDHC device to the SoC DTS.
- External 16MB QSPI flash. We are missing a whole device driver for the RA QSPI device.
